name: Publish @hauska/map-renderer

# Publishes @hauska/map-renderer to npm when a matching tag is pushed
# (e.g. map-renderer-v0.1.1), or on manual dispatch. Requires the NPM_TOKEN
# Actions secret (an npm automation token with publish rights to @hauska).
# P-318 CONTROL (falsifier 3) - not for merge. This comment exists only so that a
# pull request's diff is exactly ONE workflow file, against UNFIXED main, to show
# that no `test` check-run is produced for it. The trigger fix for this defect is
# on ci/p318-required-check-can-run; this branch deliberately carries no fix.
on:
  push:
    tags:
      - "map-renderer-v*"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Setup Node + npm registry auth
        uses: actions/setup-node@v4
        with:
          node-version: 20
          registry-url: "https://registry.npmjs.org"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build @hauska/map-renderer
        run: pnpm --filter @hauska/map-renderer build

      - name: Publish to npm
        working-directory: packages/map-renderer
        run: npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
