fix(a1b): upstream path prefix — cortex baseUrl + allowlist re-prefix

Root cause: cortex API routes live under /api/ prefix, but client
baseUrl was /api/spine/cortex, causing tile paths like /engagements
to hit upstream /engagements (SPA fallthrough → HTML not JSON).

Changes:
- cortexClient.ts: baseUrl /api/spine/cortex → /api/spine/cortex/api
- api/spine.ts: re-prefix mutation allowlist to match api/engagements
- PROXY_CONTRACT.md: update all cortex upstream paths with api/ prefix
- proxyContract.test.ts: mirror the fixed allowlist logic

Live-probe evidence in PR comment. All tests pass.
